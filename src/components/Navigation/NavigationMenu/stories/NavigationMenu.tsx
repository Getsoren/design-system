import Component, { NavigationMenuProps } from "@/components/Navigation/NavigationMenu/NavigationMenu";

const NavigationMenu = (props: NavigationMenuProps) => <Component {...props} />;

export default NavigationMenu;
